import { FilterNameRecipePipe } from './filter-name-recipe.pipe';

describe('FilterNameRecipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNameRecipePipe();
    expect(pipe).toBeTruthy();
  });
});
